auto-install-peers=false
strict-peer-dependencies=false
# node-linker=hoisted kaldırıldı: workspace alt paketlerde `vite` bin yolu kırılıyordu (Vercel + yerel build).
# Windows EPERM için: Defender dışlama veya `CI=true pnpm install --force`
