# Render cutover — tamamlandı

Bu belge [docs/TURK-ECO-GITHUB-NEON-CLOUDFLARE.md](../../docs/TURK-ECO-GITHUB-NEON-CLOUDFLARE.md) şemasına taşındı.

**Prod:** GitHub Actions + Cloudflare Worker/Container + Neon + R2.  
**Render kullanılmaz.** `render.yaml` ve `render-*.yml` arşiv/kapalıdır.
