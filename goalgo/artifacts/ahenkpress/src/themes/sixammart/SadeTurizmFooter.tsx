import { SadePublicFooter } from "./SadePublicFooter";

/** @deprecated Use `SadePublicFooter variant="turizm"` — kept for backward-compatible imports. */
export function SadeTurizmFooter() {
  return <SadePublicFooter variant="turizm" />;
}
