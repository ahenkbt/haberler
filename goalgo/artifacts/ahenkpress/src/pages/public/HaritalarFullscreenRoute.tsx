import Kesfet from "./Kesfet";

/** `/map` ve Google-benzeri `/maps/...` rotaları için gerçek tam ekran harita. */
export default function HaritalarFullscreenRoute() {
  return <Kesfet layout="fullscreen" />;
}
