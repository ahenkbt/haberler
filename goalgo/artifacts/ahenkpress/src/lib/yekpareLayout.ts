/** Canonical public content width — matches SadePublicChrome header inner container (§6). */
export const YEKPARE_CONTENT_MAX_WIDTH_PX = 1440;

/** Shared CSS utility class (see index.css `.yekpare-page-container`). */
export const YEKPARE_PAGE_CONTAINER_CLASS = "yekpare-page-container";
