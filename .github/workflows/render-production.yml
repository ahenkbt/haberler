# LEGACY — Render Production Deploy (KAPALI)
# Prod: .github/workflows/cloudflare-production.yml (GitHub + Neon + Cloudflare).
# Bu workflow kasıtlı olarak tetiklenmez.
name: Render Production Deploy (disabled)

on:
  workflow_dispatch:

jobs:
  skipped:
    runs-on: ubuntu-latest
    steps:
      - name: Render is not used
        run: |
          echo "Render production deploy is disabled. Use Cloudflare Production Deploy."
