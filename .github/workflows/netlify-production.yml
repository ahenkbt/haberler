# Netlify production KAPALI (kredi/maliyet). Üretim = Cloudflare → Render.
name: Netlify Production Deploy

on:
  workflow_dispatch:

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Netlify production disabled. Use Cloudflare Worker → Render."
