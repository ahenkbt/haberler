# LEGACY — Production is Cloudflare Worker + Container + Hostinger PostgreSQL.
# Railway is not used.
name: Railway Production Deploy (disabled)

on:
  workflow_dispatch:

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Railway production disabled. Use Cloudflare Production Deploy (GitHub + Hostinger PostgreSQL)."
