# LEGACY — Render kaldırıldı (Neon + Cloudflare Containers).
# Bu workflow bilerek devre dışı; silinebilir.
name: render-keepalive

on:
  workflow_dispatch: {}

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Render keepalive disabled — stack is Neon + Cloudflare."
