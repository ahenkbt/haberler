# LEGACY — Render keepalive (KAPALI). Render kullanılmaz.
name: render-keepalive (disabled)

on:
  workflow_dispatch:

permissions: {}

jobs:
  skipped:
    runs-on: ubuntu-latest
    steps:
      - name: Render is not used
        run: echo "Render keepalive disabled. Health checks: https://turk.eco/api/healthz"
