# LEGACY — Production is Cloudflare Worker + Container + Hostinger PostgreSQL.
# Vercel is not used.
name: Vercel Production Deploy (disabled)

on:
  workflow_dispatch:

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Vercel production disabled. Use Cloudflare Production Deploy (GitHub + Hostinger PostgreSQL)."
