# LEGACY — Render diske taşıma. Kullanmayın; media-migrate-to-r2.yml kullanın.
name: media-migrate-to-disk (disabled)

on:
  workflow_dispatch:

permissions: {}

jobs:
  skipped:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled. Use media-migrate-to-r2.yml (Cloudflare R2)."
