# Geçici production: Worker → Render (SPA+API). Docker/SPA build YOK.
# Workers Builds: Deploy = npx wrangler deploy

name = "haberler"
compatibility_date = "2025-07-15"
main = "cloudflare/worker.js"
compatibility_flags = ["nodejs_compat"]
workers_dev = true

[vars]
API_ORIGIN = "https://goalgo-y7ze.onrender.com"

# custom_domain=true → Cloudflare DNS kaydını da yönetir (www için kritik)
[[routes]]
pattern = "yekpare.net"
custom_domain = true

[[routes]]
pattern = "www.yekpare.net"
custom_domain = true
