/**
 * Ahenk Haber - Paylaşım Scripti (Tekil Sayfalar)
 */
(function () {
    // Yapışkan paylaşım çubuğu için scroll dinle
    var paylasimUst = document.querySelector('.paylasim-ust');
    if (!paylasimUst) return;
    var orijinalTop = paylasimUst.getBoundingClientRect().top + window.scrollY;
    // Temel paylaşım zaten main.js içinde - ek özellik gerekirse buraya
})();
